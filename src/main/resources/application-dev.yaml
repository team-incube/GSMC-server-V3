spring:
  config:
    activate:
      on-profile: dev
  sql:
    init:
      mode: always
security:
  cors:
    allowed-origins:
      - "*"
logging:
  level:
    org.jetbrains.exposed.sql.transactions: DEBUG